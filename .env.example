# Copy to .env and set a real password (e.g. openssl rand -hex 16).
# .env is ignored by git; scripts/create-secret.sh turns it into the postgres-secret Secret.
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
